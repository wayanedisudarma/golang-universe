version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  disable-all: true
  enable:
    - errcheck
    - govet
    - staticcheck
    - gocyclo
    - bodyclose

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0